# CGVD Revision Changelog — response-to-reviewers skeleton

Statuses: **done** | **todo-data** (needs data the author must supply/locate) | **todo-compute** (needs GPU runs, awaiting approval) | **skipped+why**.
Every item ID is from `CGVD_REVISION_BRIEF.md`; findings are in the seat reports (same folder).

## Phase 0 — Setup
| Item | Status | Notes |
|---|---|---|
| Locate LaTeX / code / logs / D | done | See `PHASE0_INVENTORY.md`. LaTeX at `paper/root.tex` (from user's Overleaf zip). D is derived from the injected asset list → oracle vocabulary confirmed; Phase 3 gate triggers. |

## Phase 1 — Tier A editing pass (all edits in `paper/root.tex` unless noted; compare against baseline commit c810075)
| Item | Status | Where | Notes |
|---|---|---|---|
| R1 superiority claim | done | Abstract, §I contrib. 3, §IV-A | "significantly outperforms state-of-the-art baselines" removed; explicit "all comparisons are against the un-augmented policies"; availability sentence added (BYOVLA public; no public DTP/OBEYED found as of Aug 2026 — verified via web search) |
| R2 headline condition | done | Abstract, §I, §IV-D | every 77.5/43.0 now reads "(π0, *put spoon on towel*, 18 semantic distractors; Table II)" |
| R3 carrot regression surfaced | done | Abstract, §I contrib. 3, §V, §VI | "prevents performance collapse" now conditioned on *semantic* clutter; regression stated in Abstract, contribution list, Limitations, Conclusion; §V "slight degradation" replaced with §IV-B's "consistently underperforms" |
| R4 OOD claim | done | §VI | deleted; "highly efficient" → "moderate (+32.8%)" |
| R5 latency wording | done | §IV-E, Table III caption, §V | one consistent description: +104 ms (+32.8%), ≈3.15→2.38 Hz; "negligible…native control frequency" removed; "negligible compared to mechanical movement time" dropped; hardware/reps = \todo (provenance unknown, see open questions) |
| R6 significance captions | done | Fig. 3 + Table II captions | phrase deleted; plain episode counts; Fig. 3 caption also warns episodes within a seed are not independent; Table I "non-monotonic variance" wording removed (CI statement todo for Phase 2) |
| R9 mechanism → hypothesis (B3) | done | Abstract, §I, §II-A, §II-D, §IV-B, Fig. 4 | all feature-dilution/attention-corruption claims now "we hypothesize"; Fig. 4 retitled "Qualitative illustration of the attention-repair hypothesis" + entailment caveat + \todo for extraction details (candidate: `scripts/visualize_pi0_attention.py` — π0 backbone hooks, softmax(QKᵀ/√d), query→256 SigLIP tokens, mean over layers+heads — author must confirm); Figs. 1, 2, 4 now cited from body; §IV-B names the two rival explanations (distribution restoration, choice-set reduction) and links mean-color-fill evidence |
| "Architecturally excluded" | done | §I, §IV-D, §III-B | → "excluded conditional on correct safe-set segmentation", cross-referenced to the refinement-ablation failure |
| R10 ablation scope | done | §IV-D | scoped to operating point; pi0-choice justification fixed ("headroom", not "more demanding testbed") |
| R11 hyperparameter table | done | §IV-A Table (tab:hyper) | all values pulled from code (see PHASE0_INVENTORY.md); availability statement = \todo URL |
| R12 selection statement | done (with \todo) | §IV-A | "fixed during development, held constant" + \todo author confirmation that no tuning used the reported cells |
| R13 Eq. (3) empty-set convention | done | §III-D | max over empty set = 0 → g = σ_safe (verified in `_compute_genuineness`); η in tab:hyper (0.30) |
| R14 protocol paragraph | done | §IV-A | success predicate, 60-step horizon, seed semantics (RNG seed; episode_id=(s+e)%24 of 24 canonical layouts; distractor seed 10⁴s+e), placement-failure rule (random fallback then hidden off-scene; episode never dropped), "no episodes excluded" (verified: 48/48 Fig.-3 results.csv have exactly 200 rows), counts {0,4,8,10,14,18} |
| R15 α definition etc. | done | §III-G | α equation + fixed-at-t0 statement; robot overwrite = only per-frame part; distilled frame = sole visual input; false-positive-erasure failure mode named incl. post-t0 intrusion; deployment scoped to static/human-excluded/fixed-camera/known-vocabulary; FK-projection named as intended real-robot path (Phase 4 sentence) |
| R8+R20 limitations rewrite | done | §V, §IV-A | limitations now: static scene (intrusion = hazard), contextual clutter regression, sim-only + two privileged inputs, single-target-instance, occlusion fabrication, startup latency; §IV-A sim-to-real bounding claim replaced with scoped version |
| R16 Table II relabel | done | Table II | row "LaMa → mean-color fill"; caption "removes or substitutes"; adversarial-patch → "we hypothesize… out-of-distribution patches… not measured" |
| R17 §II restructure | done | §II | §II-B retitled "Vision Foundation Models for Robotic Perception"; old §II-C duplication deleted; new §II-C "Adaptation and Training-Time Augmentation" covers [obeyed]+[rosie,genaug,nice] incl. NICE positioning paragraph; taxonomy now matches §I; [6] credited for distractor-type framing in §IV-A |
| R21 §II-A causal fixes | done | §II-A, §II-B, §II-D | action-discretization clause removed (π0 flow-matching, GR00T diffusion); "high-pass filter" removed, frequency language dropped; BC-IB bottleneck explicitly analogical; intriguing_vit citation removed from both disputed uses (claims now stated as conjecture without false anchor) |
| R19 M_robot/r_e, robot role | done | §III-B, §III-F | S = {c_tgt, c_anc}; robot = separate channel; M_robot + r_e defined at first use in Eq. (6) |
| R18 §IV-C type conflation | done | §IV-C | "0–4 attribute distractors"; coverage asymmetry stated (π0-only, one task, 0–4); CGVD prompt strings disclosed (verified from shell history) |
| R22 geometry qualifier | done | Abstract, §III-F, §V | "preserving background context and scene layout outside the edited regions"; t=0 occlusion-fabrication consequence in §III-F + §V |
| R23 copy-edit sweep | done | throughout + references.bib | fragment fixed, "in clutter", articles, Fig. 2 "tar"→(target, anchor), Table I headers π0/π0+CGVD, "SAM 3" unified, task names italic lowercase, robogen author list restored, Open X-Embodiment corporate author, obeyed title corrected to "Clutter-Robust" (per arXiv), author-name typo "Sangmim"→"Sangmin" in \thanks (email left as-is — author should verify) |
| Page budget | **done — 8 pages** | — | Final PDF compiles cleanly at exactly 8 pp (ICRA 6+2), zero unresolved references/citations, zero TODO markers. Cuts taken, in order: Fig. 1 dropped; Fig. 4 dropped (attention visualizations referenced to the code repo — the panel rated it evidence-free, and the mechanism is hypothesis-framed in text); hyperparameter table converted to the inline parameter list seat 2 explicitly allowed; §I/§II/§III/§IV/§V prose compressions; caption trims; figure scale-downs (methodology 0.78×, scaling 0.92×); footnotesize bibliography + abbreviated venue names. All reviewer-required content retained. |

## Phase 2 — Tier B re-analysis (script: `analysis/phase2_analysis.py`; outputs: `analysis/out/`, `paper/generated/`)
| Item | Status | Notes |
|---|---|---|
| R24 per-seed + paired analysis | done | All 48 Fig.-3 conditions: per-seed rates/deltas (`per_seed.csv`), paired t₉ CIs, Wilcoxon, per-condition ICC. **ICC ≈ 0 (median −0.02, max 0.06)** — seed clustering negligible; stated in §IV-A Analysis paragraph. (GR00T carrot-semantic-n18 lacked results.csv; episode outcomes reconstructed *exactly* from per-seed summary.csv — binary outcomes, 20 eps/seed.) |
| R25 CIs beside headline numbers | done | Ablation table now mean±SD + paired CIs. Robot-mask row **−4.5 [−10.4, +1.4] — not distinguishable from zero**; stated in §IV-D (as the methodology seat predicted). Attribute complex arm: only n4 (+16.0 [+1.2,+30.8]) excludes zero; "non-monotonic variance" language gone. |
| R26 confirmatory contrast | done | §IV-A declares π0/spoon/semantic/18 confirmatory: +34.5 pp, CI [+20.8,+48.2], paired t p=3×10⁻⁴, Wilcoxon p=0.002; all else labeled exploratory/unadjusted. |
| R27 numeric main-results table | done | `paper/generated/main_results_table.tex` (auto-generated; \input into §IV) — full 2 policies × 2 tasks × 2 types × 6 counts with mean±SD and bold CIs-excluding-zero. **GR00T and carrot finally have numbers.** "Gap widens" now has per-seed slope tests (`trend_tests.txt`): π0 spoon semantic +1.54 pp/distractor (p=4×10⁻⁴), GR00T +0.74 (p=0.02); carrot: no positive trend. §IV-B rewritten with exact values; also surfaces that *random* clutter on spoon benefits too (π0 +13.0, GR00T +17.0 at n18). |
| R28 false-erasure rate | **done (measured)** | Fresh 200-episode re-execution of the headline cell with the GT-based audit: **t=0 target erasure in 4/200 episodes (2.0%), all four complete erasures** → §IV-D now states the protection is "strong but probabilistic in practice; differs from probe approaches in failure rate, not failure mode". Bonus: the same run replicates the headline (76.0 vs 77.5 → reported in §IV-A + §IV-D). Also measured **in-flight exposure**: the moving target overlaps inpainted regions >10% in 35% of episodes → new §V paragraph (answers DA MJ6). Source: `analysis/out/new_runs_summary.txt`, `logs/replication_masks_n18/`. |
| R29 0-distractor noise floor + Table II reuse | done | n0 semantic/control cells are the *same runs* (0.0 pp diff — shared, disclosed in Fig. 3 caption + §IV-A). Real replication: π0/carrot/control/18 run twice (2026-02-17 vs 03-04): baseline 52.5 vs 49.5, CGVD 54.5 vs 54.0 — reported in §IV-A. **Table II reuse verified: π0 spoon semantic n18 logs give exactly 43.0/77.5** — stated in Table II caption. |
| R30 Table I rounding/n | **done** | Complex arm verified: 5 seeds × 20 eps (n=100 → integer grid explained); n1–4 match the printed table exactly (printed n0 85.0 vs logs 86.0 = 1 pp, unexplained, treated as re-execution variation). **Simple-arm provenance identified by experiment**: the terse instruction-consistent target ("green spoon") gives CGVD 85/80/79/75/70 (no reliable benefit), while the scene-informed descriptive target ("spoon with green handle") gives 85/79/87/82/83 — closely tracking the printed column (90/78/87/75/87). The original cloud run evidently supplied the descriptive target under the simple prompt, which encodes scene knowledge not derivable from the instruction. **Table I now reports the instruction-consistent numbers as primary**, with the descriptive-target result disclosed in §IV-C as a SAM3 prompt-sensitivity finding. Caption states 5 seeds/100 episodes. |
| Table III provenance | **done (re-measured)** | The 317 ms row was confirmed (per-inference-step forward, `vla=…ms/f`, A10G: 318.1±6.6 over 500 episodes). The 421 ms and 4,914 ms values could **not** be reproduced with the current code — today's measurements: compositing overhead 15.6±0.4 ms/frame (+5%/step, 3.14→3.00 Hz) and init 0.68±0.29 s (≤5 prompts) to ≈3.3 s (18-distractor scenes, wall-time derived). They plausibly came from an older, slower implementation. **Table III replaced wholesale with the measured values + hardware + n** (a materially better latency story than the submission claimed); Abstract/§IV-E/§V/§VI updated consistently. For the response letter: the original "+32.8%" criticism is moot under re-measurement, but we keep full disclosure of the discrepancy here. |

## Phase 3 — R7 / R32 distractor vocabulary
| Item | Status | Notes |
|---|---|---|
| R7 disclosure of D | done | §III-B + §IV-A now state D is built from the injected assets' category labels (closed vocabulary, oracle inventory); "no additional API" claim scoped to the closed-vocabulary setting; deployment story stated |
| R32 OOV condition | **done (measured)** | (First attempt invalidated by the per-episode-oracle discovery in `CGVDWrapper.reset()` — see §III-B disclosure; fixed with `freeze_distractor_names`.) Results, headline condition, same 10 seeds, CGVD arm: **vocabulary-miss D ({ladle,whisk,bowl,cup}) → 45.5%**, statistically = baseline 43.0 (Δ +2.5 [−6.6,+11.6]); **generic 12-category tabletop D → 76.5%**, statistically = oracle D (Δ +0.5 [−4.7,+5.7]; vs baseline +33.5 [+22.3,+44.7]). **Conclusion in §IV-D: the gain requires vocabulary *coverage*, not the exact inventory — a plausible site vocabulary fully recovers the headline; a miss forfeits it.** Answers seat2 W3/Q1, seat3 W2/Q1, seat4 W4/Q2, DA C1 with data. |

## Phase 4 — R33 SAM3 robot mask
| Item | Status | Notes |
|---|---|---|
| R33 SAM3-mask substitution + timing | **done (measured)** | Full pipeline with per-frame SAM3 robot mask, same 10 seeds: **SR 74.0%** vs 76.0 GT-mask re-execution (paired Δ −2.0 pp, CI [−11.3,+7.3]; vs original 77.5: −3.5 [−12.7,+5.7]) — accuracy statistically unchanged. Cost: **240.7±1.9 ms/frame** (control rate → ≈1.7 Hz) and **20.1% of frames with no robot detection** (stale-mask fallback). New "Deployable robot mask" paragraph in §IV-D; §III-G and §V updated: the renderer mask's privilege is latency/reliability, not accuracy; FK projection remains the hardware path. Data: `logs/robotmask_sam3_n18/`, 12,400 frame-level latency samples. |

## Phase 5 — R31 BYOVLA comparator
| Item | Status | Notes |
|---|---|---|
| R31 BYOVLA on same setup | **done (measured)** — Full run complete (10 matched seeds × 20 eps, `logs/byovla_n18`): **BYOVLA 32.5%** vs baseline 43.0 (Δ −10.5 [−25.7,+4.7], n.s.) and vs CGVD 77.5 (paired **Δ −45.0 [−56.4,−33.6]**); intervention cost 11.3±2.0 s/chunk (~33 π0 forwards) ≈ **34× CGVD's total per-step cost**; probe discriminates (mean 2.0/32.1 regions flagged, range 0–16; 3,000 chunks logged). New §IV-E "Comparison with a Run-Time Intervention Baseline" reports SR + wall-clock with full caveats (our reimplementation with 3 disclosed confound-removing substitutions; 18 distractors outside BYOVLA's original regime; threshold untuned beyond reference 0.002 m; conclusion scoped to "per-chunk probe-and-inpaint does not scale to dense clutter"). Measured comparative sentences reinstated in Abstract + §I + §VI per branch B1. Mechanistic account ties the failure to per-chunk re-inpaint flicker — the instability CGVD's cached clean plate avoids (validated by the mean-color-fill ablation). Prep note: | Public code found (`github.com/irom-princeton/byovla`) — a real-robot Octo reference implementation using GPT-4o + GroundingDINO/SAM2 + LaMa. Reimplemented for SimplerEnv+π0 in `src/cgvd/byovla_intervention.py` with three **disclosed substitutions**: (1) GPT-4o → the same closed vocabulary D CGVD uses (no API key available here; also removes the VLM-quality confound → vocabulary-matched comparison), (2) GroundingDINO+SAM2 → SAM 3 (shared with CGVD), (3) Octo fixed-PRNGKey sampling → π0 fixed-torch-seed probes (N=1 deterministic vs reference N=5). Kept faithful: per-object Gaussian-blur perturbation (kernel 15–30), translation-only weighted delta w=[1,1,1,0,0,0,0], threshold 0.002 m on unnormalized actions, inpaint dilation 10, intervention before every action chunk. Run: `run_revision_queue3.sh smoke` then `full` (~8–13 h; wall-clock per chunk logged for the efficiency comparison). |

## Formerly-open questions — resolved by investigation (author said they couldn't recall; confirmed from evidence 2026-08-26)
1. **Table I / missing Simple arm:** searched the whole disk, the `upstream/results` branch (old paper-draft text only), and `~/logs.zip.tmp` (an empty, failed RunPod download from Mar 2) — the Simple-arm logs and the source of the printed Complex-n0 85.0 do **not** exist on this machine; they were cloud-only. Resolution: both arms re-run locally (queue job 1); Table I will be regenerated entirely from the reproducible local runs, and the 1-pt difference vs the arXiv table at Complex-n0 (86.0 local vs 85.0 printed) is treated as run-to-run variation across re-executions, noted here for the response letter.
2. **Fig. 4 provenance: CONFIRMED.** The figure PNGs' panel layout and titles ('Input' / 'Attention (16x16)' / 'Overlay') exactly match `src/utils/attention_capture.py::save_attention_map` — i.e., Fig. 4 was produced by `batch_eval.py --save_attention` during eval runs: Q/K hooks on every self-attention layer of π0's PaliGemma backbone, softmax(QKᵀ/√d) recomputed, final-query-token attention onto the 256 SigLIP tokens, averaged over all layers and heads, captured at the first inference step (baseline arm = raw frame, CGVD arm = distilled frame). Caption updated with this method; \todo removed. (The exact source episode is not recoverable — the PNGs were cloud-generated; the figure is labeled a single representative frame.)
3. **Hyperparameter selection (R12): CONFIRMED from git history.** Core values (blend σ=3.0, r_d=11, thresholds) date to commits 099500e (2026-02-08) / 78d0d17 (2026-02-10); the only later value change is `safeset_warmup_frames` 5→1 in bdb9928 (2026-02-17 00:26 UTC), which precedes the earliest kept run batch (2026-02-17 09:53) and all March runs. So parameters were frozen before every reported run; §IV-A now states this with the version-control anchor. Pilot tuning used small debug rollouts (episodes ≤ 10, mostly spoon task — visible in shell history), disclosed as such.
4. **Table III provenance: largely resolved.** The live eval log prints `vla=317–323 ms/f` per episode (per-inference-step forward time) on this A10G — matching Table III's 317 ms baseline row; hardware = NVIDIA A10G (the same class used in the March runs' Docker setup). Dispersion will be computed from the queue logs; Table III caption gets hardware + dispersion when job 1 finishes.
5. **Code availability:** `github.com/awd1779/pi0` is public (verified via GitHub API) — cited in §IV-A as the availability statement.

## Final deliverable status (2026-08-27)
1. **Revised LaTeX**: `paper/root.tex`, compiles cleanly at **8 pages**, branch `revision-panel-r1`, pushed to `github.com/awd1779/pi0`.
2. **Analysis scripts + generated tables**: `analysis/` + `paper/generated/` — committed.
3. **This changelog**: complete R-item → change → status mapping (response-letter skeleton).
4. **Open TODOs for the author**:
   - The public branch exposes the unpublished paper + referee reports (repo is public) — decide whether to keep, make private, or split.
   - Verify the `\thanks` email (`Sangmim.song@uts.edu.au`) — the name typo was fixed in text but the address was left as-is.
   - Table I complex-arm n0 cell: logs give 86.0, arXiv printed 85.0 (1 pt, unexplained; log-derived value now used).
   - Optional strengthening (not required by the panel): third architecture (OpenVLA — harness exists), real-robot demo, title "Overcoming"→"Mitigating" (venue seat's suggestion; not applied).
   - Fig. 4 was cut for the page limit; restore it only by trading ~0.4 page elsewhere.
   - Keep `github.com/awd1779/pi0` public (it is cited as the availability statement); consider tagging the submission commit.

---

# Round 2 — Punch-list pass (F1–F8, branch `punchlist-r2`, 2026-08-28)

| Item | Status | What changed |
|---|---|---|
| **F7 page budget** | **done — 8 pages** | Compiled first (8.0 at start; F1–F6 additions pushed to 9; recovered via the brief's sanctioned trims (i)–(iv): §IV-E mechanism list → footnote, §IV-D/§III-A/§III-F dedup, worked spatula example cut, §IV-C digression merged into the F4 disclosure — plus caption/prose compression, figure scale-downs, bib author-list compaction (3 + *et al.*), standard IEEE venue abbreviations, and tightened bibliography item spacing. 0 errors, 0 unresolved refs/citations. |
| **F8.1 OBEYED-VLA** | done | (a) Re-described accurately in §I (trained perception module producing task-conditioned, object-centric, geometry-grounded observations; policy fine-tuned on clean demos), removed from §II-C (retitled "Robustness via Training-Time Augmentation") and placed beside BYOVLA in §II-D as observation-space prior work with the real distinction (CGVD trains/fine-tunes nothing); [8] dropped as anchor for the feature-dilution hypothesis. (b) Availability sentence rescoped exactly per brief — verified myself: github.com/UARK-AICV/OBEYED_VLA exists (created 2025-12-28), contents = perception module only (perception_core/service, VLM mask selection, Cutie, DepthAnythingV2; no policy/robot interface). |
| **F8.2 taxonomy credit** | done | §IV-A now claims the Semantic/Random/Attribute taxonomy as ours, citing [6] only for degradation magnitude (noting its continuous clutter measure); §I similarity-concentration claim softened to "consistent with prior reports [6]". |
| **F8.3 Eva-VLA** | done | Shouwei Ruan restored as second author in the bib; [7] removed from both clutter-degradation citation pairs and recategorized in §I as "broader physical variations". |
| **F8.4 GR00T N1 vs N1.6** | done | Footnote at §IV-A Policies: [4] documents N1, evaluated checkpoint is the N1.6 revision (different backbone/action head), NVIDIA model card as secondary reference. |
| **F8.5 repo** | done | README gained a CGVD section; new `CGVD.md` (code map, reproduction commands, control = paper's *Random* mapping); **code released to `main`** (commit 2472802, pushed): BYOVLA reimplementation, `--cgvd_distractor_names` + `--cgvd_robot_mask_source` + `--eval_byovla` flags, erasure audit, analysis + queue scripts — §IV-E and the vocabulary study are now reproducible from the released artifact. |
| **F1 front-matter register** | done | Abstract BYOVLA sentence carries the design-envelope caveat + corrected cost basis; §VI adds the envelope/threshold-untuned clause and per-chunk cost basis; Abstract/§I/§VI attribute claims rescoped to "one exploratory 5-seed condition"; §IV-E baseline delta restated as "numerically below … within noise" and the "one-sided" opener removed. |
| **F2 D description** | done | Abstract: "parses the instruction into a safe set, applies a supplied distractor vocabulary…"; closing sentence adds "with a known clutter vocabulary"; §III key insight scoped to the safe set with the vocabulary supplied separately (§III-A). |
| **F3 comparator auditability** | done | Inpainter named (the *shared* LaMa — verified in `src/cgvd/byovla_intervention.py`); chunk length stated (4 steps, from `config/eval/bridge.yaml` `act_steps: 4`, matching the reference's `n_steps=4`); **34× corrected to ≈30× with the basis explicit** (per 4-step chunk: 11.6 s vs 0.38 s). The same audit exposed two more basis errors, fixed: "+5%/step" → per-chunk +20% / ≈+10% episode wall time (9.11→10.00 s, measured), and the robot-mask "≈1.7 Hz" → ≈0.96 s per chunk. Table IV rows relabeled "per inference step (one per chunk)". |
| **F4 re-measurement disclosures** | done | §IV-F: one sentence noting the earlier version reported a single blended per-step figure, now replaced by decomposed timings (provenance of the exact 421 ms → ASK-AUTHOR). §IV-C: re-execution of both arms disclosed with the reason (removal of the scene-informed-prompt asymmetry, "a correction against our own interest") and the 5-seed protocol note. |
| **F5 verifiability** | done | 12-category vocabulary enumerated in §IV-D (from `analysis/run_revision_queue2b.sh`); §III-A forward-ref scoped to the tested operating point; **realized-vs-nominal counts measured from logs** (1,400 audited n=18 episodes: 18.7% below nominal, mean 17.6, ≥16 in 96% — disclosed in §IV-A with the conservative-direction note); 35% audit protocol fully specified in §V (200-episode headline re-execution, CGVD arm, renderer GT target mask vs fixed inpaint mask, >10% max per-frame overlap); 20.1% given its denominator (12,400 frames); **≈3.3 s init figure replaced by direct measurement: 0.91±0.02 s steady-state at n=18** (5-episode instrumented run; the wall-derived figure had conflated env-stepping residual and one-time model loading — Abstract/§IV-F/§V/Table IV all updated to 0.7–0.9 s). |
| **F6 deployment implications** | done | §IV-D: vocabulary-miss is not a no-op (scene still edited — mask ≈2% vs ≈14% oracle, measured; cache frozen; risks live; 45.5% = forfeited benefit) + the free near-empty-mask miss detector; robot-mask fallback consequence sentence (leading edge unprotected, no staleness bound, FK avoids the class); equivalence over-read replaced by the calibrated form ("no accuracy difference detected; excludes costs larger than ≈11 pp"). |

## ASK-AUTHOR (round 2)
1. **v1 latency provenance**: the paper now states neutrally that the earlier version reported a blended per-step figure. The arithmetic 318 + 4914/60 + 16 ≈ 416 ≈ 421 suggests v1 folded amortized initialization into the per-step average, but neither git history nor logs prove how 421/4,914 ms were produced. Confirm (or leave the neutral sentence as is).
2. **Table II n0 (Complex baseline)**: printed v1 said 85.0; logs give 86.0 — unresolved 1 pt (documented in round 1).
3. **Optional compute (brief-flagged)**: extending the attribute study (both arms) from 5 → 10 seeds (~2,000 episodes ≈ 6–7 h GPU) would firm up the Abstract's attribute claim. Say the word and I'll queue it.
4. Public-repo exposure of `paper/` + `review panel/` on the pushed branches remains your call (from round 1).

## Style-alignment pass (author request, 2026-08-28)
Compared against the author's original draft (`review panel/overleaf_source_v2.zip`, byte-identical to the v1 baseline) and rewrote the revision-added text in the author's register: narrative connectives ("First…", "Conversely…", "This likely arises because…", "This suggests that…"), full sentences instead of compressed clause chains, and far fewer inline statistics. Specifically:
- §IV-B (the passage the author flagged): per-condition numbers and CIs removed from prose — the table carries them; prose keeps only the two trend slopes and the narrative.
- §IV-C: per-cell repeats removed (Table II carries them); the re-execution disclosure folded in naturally.
- §IV-D/§IV-E: paragraphs rewritten as flowing prose; every measured number retained exactly once.
- **Table IV (latency) deleted as redundant** — §IV-F's prose already narrated every value; it now carries all of them (means±SD, n, hardware) once. The §V startup sentence duplicating §IV-F was also removed, as was §V's duplicate of §VI's when-to-distill future-work point.
- Em-dash chains, "The key insight:"-style colon constructions, telegraphic bullet phrasing, and stat-parentheticals reduced throughout; the author's signature sentences restored (e.g., the §I "promise a future" line, "Future work will explore…").
- No caveat, negative result, or measured value was removed — only relocated to its single canonical home.
Recompiled at exactly **8 pages**, 0 errors, 0 unresolved references.
